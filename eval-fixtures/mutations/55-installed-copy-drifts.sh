# expect: install-in-sync
# class:  declared-not-wired
# origin: self-audit (the live copy and the repo diverged silently for a week)
printf '\n\nEdited in the install only.\n' >> "$HOME/.claude/skills/keel-workflow/SKILL.md"
