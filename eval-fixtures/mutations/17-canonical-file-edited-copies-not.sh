# expect: rule-text-verbatim
# class:  declared-not-wired
# origin: design of the canonical mechanism
printf 'Do not pass a `model` override at any call site — the agent file pins it.\n' > rules/no-model-override.txt
