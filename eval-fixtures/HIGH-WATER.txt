# Monotonic floor. These may only go UP.
#
# Every consistency check in this repo is an equality between two sets —
# ondisk == registry, declared == actual, tsv rows == RULE-INVENTORY rows. An
# equality survives deleting the same member from both sides, so the whole
# apparatus detects divergence and is structurally blind to SHRINKAGE. A rule,
# its check, its mutation and its inventory row can leave together with every
# board green: the seventh audit did exactly that to backward route A6 and to
# the readme-shell-grant check, and nothing noticed.
#
# Equality is the wrong direction for this. A floor is the right one.
#
# To remove something deliberately: lower the number here AND add a line
#   shrink: <what> <why> <date>
# below. The check prints that justification every run, so a removal is a
# visible, named act instead of an arithmetic coincidence.
checks	23
mutations	72
rules	96
routes	7
gates	9
agents	16
fixtures	23
